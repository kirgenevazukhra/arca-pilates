# ARCA — дизайн-система студии пилатеса на реформерах

Собрана по четырём референсам (студия с арками и реформерами, коллаж с девушкой,
аэро-студия с тремя арками, студия с лаймвош-стенами). Ниже — что у них общего
и как это переведено в правила для сайта.

---

## 1. Что общего у референсов

### Палитра
- **Тёплые нейтралы без единого холодного тона.** Стены — песок/крем/бежевая
  штукатурка, пол — светлое дерево или терраццо, оборудование — бежевая кожа
  и светлый дуб.
- **Один тёмный акцент** — почти чёрные ремни, гантели, потолочные балки.
  Это единственный контраст, и он всегда тонкий и линейный.
- **Свет как цвет.** Тёплая LED-подсветка внутри арок даёт «гало» — светлое
  янтарное свечение по контуру. Это главный визуальный приём всех четырёх фото.
- Контраст низкий, тени мягкие, ни одной чистой белой или чёрной поверхности.

### Форма и «типографика» пространства
- **Арка — ключевой мотив.** Ниши, зеркала, дверные проёмы — всё с полукруглым
  верхом. Арки повторяются рядами по три и более.
- Прямые линии присутствуют только как тонкие «штрихи»: балки, ремни, рейлинги
  реформеров. Ощущение — как у высококонтрастного серифа: тонкие линии + мягкие
  округлые формы.
- Текстуры матовые: штукатурка, лён, замша, дерево. Ничего глянцевого.

### Сетка и ритм
- **Ритм повторяющихся модулей.** Реформеры стоят в ряд с одинаковым шагом,
  арки идут серией — это горизонтальная сетка из равных колонок.
- **Три горизонтальных слоя**: потолок (тёмные балки/бетон), стена (светлая
  штукатурка + арки), пол (дерево/терраццо). Соотношение примерно 1 : 2 : 1.
- Симметрия и центр: в кадре всегда есть ось — центральная арка, ковёр,
  фигура посередине.

### Пространство
- Много «воздуха»: предметов мало, они мелкие относительно стены.
- Фигура человека — единственный динамичный элемент, всегда на фоне арки или
  ряда реформеров. Она задаёт движение в статичной сцене.
- Взгляд ведут в глубину: анфилада арок, перспектива ряда реформеров.

---

## 2. Токены

### Цвета
| Токен | HEX | Роль |
|---|---|---|
| `--cream-0`   | `#FBF8F3` | Фон страницы, «свет стены» |
| `--sand-100`  | `#F3EDE3` | Секции, карточки |
| `--sand-200`  | `#E8DFD1` | Границы, разделители, подложки |
| `--sand-300`  | `#D9CDBB` | Ховер-состояния, тени в штукатурке |
| `--taupe-500` | `#B39E86` | Вторичный текст, иконки |
| `--taupe-700` | `#7E6B58` | Подписи, кожа реформеров |
| `--umber-900` | `#3E342C` | Основной текст, балки, ремни |
| `--ink`       | `#26201B` | Заголовки, футер |
| `--halo`      | `#F5D9A6` | Свечение арок, акцент |
| `--halo-soft` | `#FBEBCB` | Мягкий градиент подсветки |
| `--clay`      | `#C8A27B` | Кнопки, линии, hover-акцент |

Правила: не использовать чистые `#fff` и `#000`. Любой контраст даётся
цветом `--umber-900` на `--cream-0`. Тени — только тёплые:
`0 20px 60px -20px rgba(62,52,44,.25)`.

### Типографика
- **Display / заголовки:** `Cormorant Garamond` (серif с высоким контрастом
  штриха — эквивалент тонких ремней на мягкой штукатурке). Начертания 300–500,
  активно используется italic. Трекинг заголовков `-0.01em`, для капса `0.18em`.
- **Текст / UI:** `Manrope` 300–500. Спокойный геометрический гротеск,
  не спорит с серифом.
- Шкала (rem): 0.75 / 0.875 / 1 / 1.25 / 1.75 / 2.5 / 4 / 7 / 12 (fluid через
  `clamp`).
- Межстрочное: заголовки 0.95–1.05, текст 1.6.
- Надзаголовки — капс, 0.75rem, трекинг 0.22em, цвет `--taupe-500`.

### Сетка
- Контейнер `min(1320px, 92vw)`, 12 колонок, gutter `clamp(16px, 2vw, 32px)`.
- Модульный ритм «ряд реформеров»: карточки в 3 колонки одинаковой ширины,
  на мобильных — горизонтальный скролл, шаг сохраняется.
- Вертикальный ритм секций: `clamp(96px, 12vw, 180px)`.
- Композиция секции — трёхслойная, как интерьер: тонкая «балка» сверху
  (линия/надзаголовок), тело (арки/контент), «пол» (подпись/CTA).

### Форма
- Радиус арки: `999px 999px 0 0` — основной радиус для медиа и карточек.
- Малый радиус для кнопок и инпутов: `999px` (pill).
- Обводки: `1px solid var(--sand-200)`.
- Свечение арки: `box-shadow: inset 0 0 0 1px var(--halo), 0 0 60px -10px var(--halo)`.

### Движение
- Кривая: `cubic-bezier(.16,1,.3,1)` — плавная, «дыхательная».
- Длительности: micro 300ms, reveal 900ms, амбиент 6–12s (бесконечно).
- Приёмы: медленное «дыхание» гало (opacity/scale), Ken Burns для фото
  интерьера, line-art-фигура с циклической анимацией ног в ремнях,
  reveal-по-скроллу снизу вверх на 24px, бегущая строка.
- Уважать `prefers-reduced-motion`: анимации отключаются.

---

## 3. Компоненты
- **Nav** — прозрачная, логотип серифом, ссылки капсом, CTA-пилюля.
- **Hero** — CSS-сцена интерьера: три арки с гало + реформер + анимированная
  фигура. Название студии крупным серифом поверх.
- **Marquee** — тонкая лента с капсом и точками-разделителями.
- **Arch Card** — карточка-арка: медиа сверху с арочным радиусом, подпись снизу.
- **Photo Block** — контейнер с арочной маской под фото интерьера,
  Ken Burns при появлении.
- **Figure Stage** — SVG line-art фигуры на реформере, анимация цикла.
- **Price Card** — три равных колонки, серифные цифры.
- **Footer** — огромное название на всю ширину, тёмный `--ink`.
